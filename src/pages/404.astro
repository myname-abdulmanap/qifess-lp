---
// 404.astro
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>404 - Page Not Found</title>
    <style>
      :root {
        --color-primary: #ffd502;
        --color-text: #1f2937;
        --color-text-light: #6b7280;
        --color-background: #f9fafb;
        --font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      }

      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
      }

      body {
        font-family: var(--font-family);
        background-color: var(--color-background);
        color: var(--color-text);
        display: flex;
        align-items: center;
        justify-content: center;
        height: 100vh;
        text-align: center;
        padding: 0 20px;
      }

      .container {
        max-width: 600px;
      }

      .error-code {
        font-size: 120px;
        font-weight: 900;
        color: var(--color-primary);
        line-height: 1;
        margin-bottom: 24px;
      }

      h1 {
        font-size: 28px;
        margin-bottom: 16px;
      }

      p {
        color: var(--color-text-light);
        font-size: 18px;
        line-height: 1.6;
        margin-bottom: 32px;
      }

      .button {
        display: inline-block;
        background-color: var(--color-primary);
        color: white;
        font-weight: 600;
        text-decoration: none;
        padding: 12px 24px;
        border-radius: 6px;
        transition: all 0.2s ease;
      }

      .button:hover {
        background-color: #4338ca;
        transform: translateY(-2px);
      }

      .spacer {
        height: 50px;
      }

      .sad-face {
        font-size: 60px;
        margin-bottom: 24px;
        opacity: 0.5;
      }

      .countdown {
        margin-top: 24px;
        font-size: 16px;
        color: var(--color-text-light);
      }

      .loader {
        margin: 12px auto;
        width: 250px;
        height: 6px;
        background-color: #e5e7eb;
        border-radius: 3px;
        overflow: hidden;
      }

      .loader-bar {
        height: 100%;
        width: 0%;
        background-color: var(--color-primary);
        animation: loading 5s linear forwards;
      }

      @keyframes loading {
        0% { width: 0%; }
        100% { width: 100%; }
      }

      @media (max-width: 640px) {
        .error-code {
          font-size: 100px;
        }
        
        h1 {
          font-size: 24px;
        }
        
        p {
          font-size: 16px;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="sad-face">😕</div>
      <div class="error-code">404</div>
      <h1>Page Not Found</h1>
      <p>
        Sorry, the page you're looking for cannot be found or has been moved.
        You might have mistyped the URL or the page may no longer be available. - abdulmanap.com
      </p>
      <a href="/" class="button">Back to Homepage</a>
      
      <div class="countdown">
        Redirecting to homepage in <span id="countdown-seconds">5</span> seconds...
      </div>
      <div class="loader">
        <div class="loader-bar"></div>
      </div>
      
      <div class="spacer"></div>
    </div>

    <script is:inline>
     
      let secondsLeft = 5;
      const countdownElement = document.getElementById('countdown-seconds');
      
      const countdown = setInterval(() => {
        secondsLeft -= 1;
        if (countdownElement) {
          countdownElement.textContent = String(secondsLeft); 
        }
        
        if (secondsLeft <= 0) {
          clearInterval(countdown);
          window.location.href = '/';
        }
      }, 1000);
    </script>
  </body>
</html>