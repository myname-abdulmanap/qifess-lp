<!-- Hero/About Section -->
<section class="hero">
  <div class="hero-container">
    <div class="hero-content">
      <div class="hero-text">
        <br/>
        <br />
        <br />
        <span class="typing-text fade-in delay-1"
          >About QIFESS<span class="peace"></span></span
        >

        <p class="fade-in delay-2">
          QIFESS was developed based on our extensive experience in the field-services industry. We
          understand the challenges faced by technicians and companies in managing various projects
          with different business requirements.
        </p>

        <div class="cta-buttons-left fade-in delay-3">
          <a href="#features" class="btn-primary">Explore Features</a>
          <a href="/contact" class="btn-secondary">Get Started</a>
        </div>
      </div>

      <div class="hero-image fade-in delay-4">
        <img src="/public/assets/img/qifess-mock2.png" alt="" srcset="">
        <!-- <dotlottie-player
          src="https://lottie.host/dfae2e7d-906f-48f2-b921-e258a0aa1038/ilp8vqa0jD.lottie"
          background="transparent"
          speed="1"
          style="width: 400px; height: 400px"
          loop
          autoplay
        >
        </dotlottie-player> -->
      </div>
    </div>
  </div>
</section>

<!-- Features Section -->
<section class="features" id="features">
  <div class="container">
    <div class="section-header">
      <h2 class="section-title">Why Choose QIFESS?</h2>
      <p class="section-subtitle">One integrated solution for all your field service needs</p>
    </div>

    <div class="features-grid">
      <div class="feature-card fade-in">
        <div class="feature-icon"></div>
        <h3>Unified Application</h3>
        <p>
          QIFESS provides an integrated solution that allows technicians to use just one application
          for all projects, regardless of the complexity of their business processes.
        </p>
      </div>

      <div class="feature-card fade-in">
        <div class="feature-icon"></div>
        <h3>Flexible Customization</h3>
        <p>
          With its flexible customization features, QIFESS can be tailored to meet the specific
          needs of each project, ensuring optimal operational efficiency.
        </p>
      </div>

      <div class="feature-card fade-in">
        <div class="feature-icon"></div>
        <h3>Real-time Monitoring</h3>
        <p>
          QIFESS is equipped with real-time monitoring capabilities that track technician
          productivity and task progress in the field.
        </p>
      </div>

      <div class="feature-card fade-in">
        <div class="feature-icon"></div>
        <h3>Location Tracking</h3>
        <p>
          Companies can easily monitor technician locations and work status, ensuring that all tasks
          are executed efficiently.
        </p>
      </div>

      <div class="feature-card fade-in">
        <div class="feature-icon"></div>
        <h3>Analytics Dashboard</h3>
        <p>
          All data is accessible from the Head Office through an intuitive Dashboard, which serves
          as a powerful tool for analysis and decision-making.
        </p>
      </div>

      <div class="feature-card fade-in">
        <div class="feature-icon"></div>
        <h3>Strategic Partnership</h3>
        <p>
          QIFESS is more than just a tool—it is a strategic partner for businesses looking to
          streamline their field operations.
        </p>
      </div>
    </div>
  </div>
</section>

<!-- Benefits Section -->
<section class="benefits">
  <div class="container">
    <div class="benefits-content">
      <div class="benefits-text">
        <h2>Transform Your Field Operations</h2>
        <p>
          QIFESS empowers your business to achieve operational excellence through advanced
          technology and intelligent automation.
        </p>

        <div class="benefits-list">
          <div class="benefit-item">
            <span class="benefit-icon">✓</span>
            <div>
              <h4>Improved Productivity</h4>
              <p>
                Boost workforce productivity with streamlined processes and automated workflows.
              </p>
            </div>
          </div>

          <div class="benefit-item">
            <span class="benefit-icon">✓</span>
            <div>
              <h4>Enhanced Service Quality</h4>
              <p>
                Deliver superior service quality with real-time updates and comprehensive
                monitoring.
              </p>
            </div>
          </div>

          <div class="benefit-item">
            <span class="benefit-icon">✓</span>
            <div>
              <h4>Operational Efficiency</h4>
              <p>
                Optimize your field operations with data-driven insights and intelligent resource
                allocation.
              </p>
            </div>
          </div>
        </div>
      </div>

      <div class="benefits-visual">
        <div class="stats-grid">
          <div class="stat-card">
            <h3>95%</h3>
            <p>Efficiency Increase</p>
          </div>
          <div class="stat-card">
            <h3>24/7</h3>
            <p>Real-time Monitoring</p>
          </div>
          <div class="stat-card">
            <h3>100%</h3>
            <p>Customizable</p>
          </div>
          <div class="stat-card">
            <h3>∞</h3>
            <p>Scalable Projects</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- CTA Section -->
<section class="cta-section">
  <div class="container">
    <div class="cta-content">
      <h2>Ready to Transform Your Field Operations?</h2>
      <p>
        Join hundreds of companies already using QIFESS to optimize their field service management.
      </p>
      <div class="cta-buttons">

        <a href="/contact" class="btn-secondary large">Schedule Demo</a>
      </div>
    </div>
  </div>
</section>

<style>
  /* Hero Section */
  .hero {
    background: var(--container-color);
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding: 3rem 1rem;
    position: relative;
    overflow: hidden;
  }

  .hero-container {
    max-width: 1200px;
    width: 100%;
    margin: 0 auto;
    color: var(--text-header);
    position: relative;
    z-index: 1;
  }

  .hero-content {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 3rem;
  }

  .hero-text {
    flex: 1;
    text-align: left;
  }

  .hero-image {
    flex: 1;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .typing-text {
    font-weight: bold;
    font-size: 3rem;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.1);
    display: block;
    margin-bottom: 1.5rem;
    color: var(--heading-color);
  }

  .peace {
    display: inline-block;
    transition: transform 0.3s ease;
  }

  .typing-text:hover .peace {
    transform: rotate(15deg);
  }

  .hero p {
    font-size: 1.2rem;
    max-width: 600px;
    margin: 0 0 2rem 0;
    color: var(--text-color);
    line-height: 1.6;
  }

  /* Buttons */
  .cta-buttons {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    justify-content: center;
    margin-top: 2rem;
  }

  .cta-buttons-left {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    justify-content: flex-start;
    margin-top: 2rem;
  }

  .btn-primary,
  .btn-secondary {
    padding: 1rem 2rem;
    border-radius: var(--border-radius);
    text-decoration: none;
    font-weight: 600;
    transition: var(--transition);
    display: inline-block;

  }

  .btn-primary {
    background: var(--primary);
    color: white;
    border: 2px solid var(--primary);
  }

  .btn-primary:hover {
    background: var(--primary-dark);
    transform: translateY(-2px);
  }

  .btn-secondary {
    background: transparent;
    color: var(--heading-color);
    border: 2px solid var(--heading-color);
  }

  .btn-secondary:hover {
    background: var(--heading-color);
    color: var(--bg-color);
  }

  .btn-primary.large,
  .btn-secondary.large {
    padding: 1.2rem 2.5rem;
    font-size: 1.1rem;
  }

  /* Container */
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1rem;
  }

  /* Features Section */
  .features {
    background: var(--color-features);
    padding: 5rem 0;
  }

  .section-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .section-title {
    font-size: 2.5rem;
    color: var(--heading-color);
    margin-bottom: 1rem;
    font-weight: 700;
  }

  .section-subtitle {
    font-size: 1.2rem;
    color: var(--text-color);
    max-width: 600px;
    margin: 0 auto;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 2rem;
  }

  .feature-card {
    background: var(--color-card);
    padding: 2rem;
    border-radius: var(--border-radius);
    text-align: center;
    transition: var(--transition);
    border: 1px solid rgba(255, 255, 255, 0.1);
  }

  .feature-card:hover {
    transform: translateY(-5px);
    box-shadow: var(--shadow-lg);
  }

  .feature-icon {
    font-size: 3rem;
    margin-bottom: 1rem;
  }

  .feature-card h3 {
    color: var(--heading-color);
    margin-bottom: 1rem;
    font-size: 1.3rem;
  }

  .feature-card p {
    color: var(--text-color);
    line-height: 1.6;
  }

  /* Benefits Section */
  .benefits {
    background: var(--bg-main);
    padding: 5rem 0;
  }

  .benefits-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .benefits-text h2 {
    font-size: 2.5rem;
    color: var(--heading-color);
    margin-bottom: 1rem;
  }

  .benefits-text > p {
    color: var(--text-color);
    font-size: 1.2rem;
    margin-bottom: 2rem;
    line-height: 1.6;
  }

  .benefits-list {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .benefit-item {
    display: flex;
    gap: 1rem;
    align-items: flex-start;
  }

  .benefit-icon {
    background: var(--primary);
    color: white;
    width: 24px;
    height: 24px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.8rem;
    flex-shrink: 0;
    margin-top: 0.2rem;
  }

  .benefit-item h4 {
    color: var(--heading-color);
    margin-bottom: 0.5rem;
  }

  .benefit-item p {
    color: var(--text-color);
    line-height: 1.5;
  }

  .stats-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
  }

  .stat-card {
    background: var(--color-card);
    padding: 2rem;
    border-radius: var(--border-radius);
    text-align: center;
    border: 1px solid rgba(255, 255, 255, 0.1);
  }

  .stat-card h3 {
    font-size: 2.5rem;
    color: var(--primary);
    margin-bottom: 0.5rem;
    font-weight: 700;
  }

  .stat-card p {
    color: var(--text-color);
    font-size: 0.9rem;
  }

  /* CTA Section */
  .cta-section {
    background: linear-gradient(135deg, var(--primary), var(--secondary-color));
    padding: 5rem 0;
    text-align: center;
  }

  .cta-content h2 {
    font-size: 2.5rem;
    color: white;
    margin-bottom: 1rem;
  }

  .cta-content p {
    font-size: 1.2rem;
    color: rgba(255, 255, 255, 0.9);
    margin-bottom: 2rem;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .cta-section .btn-primary {
    background: white;
    color: var(--primary);
  }

  .cta-section .btn-primary:hover {
    background: rgba(255, 255, 255, 0.9);
    transform: translateY(-2px);
  }

  .cta-section .btn-secondary {
    background: transparent;
    color: white;
    border: 2px solid white;
  }

  .cta-section .btn-secondary:hover {
    background: white;
    color: var(--primary);
  }

  /* Animations */
  .fade-in {
    opacity: 0;
    transform: translateY(20px);
    animation: fadeIn 0.8s ease forwards;
  }

  @keyframes fadeIn {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .delay-1 {
    animation-delay: 0.2s;
  }
  .delay-2 {
    animation-delay: 0.4s;
  }
  .delay-3 {
    animation-delay: 0.6s;
  }
  .delay-4 {
    animation-delay: 0.8s;
  }

  /* Responsive Design */
  @media (max-width: 992px) {
    .hero-content {
      flex-direction: column;
      text-align: center;
    }

    .benefits-content {
      grid-template-columns: 1fr;
      gap: 3rem;
    }

    .features-grid {
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    }
  }

  @media (max-width: 768px) {
    .typing-text {
      font-size: 2.2rem;
    }

    .section-title {
      font-size: 2rem;
    }

    .hero p,
    .benefits-text > p,
    .cta-content p {
      font-size: 1rem;
    }

    .hero {
      padding: 2rem 1rem;
    }

    .features,
    .benefits,
    .cta-section {
      padding: 3rem 0;
    }

    .cta-buttons {
      flex-direction: column;
      align-items: center;
    }

    .btn-primary,
    .btn-secondary {
      width: 100%;
      max-width: 300px;
      text-align: center;
    }

    .stats-grid {
      grid-template-columns: 1fr;
    }
  }
</style>

<script
  src="https://unpkg.com/@dotlottie/player-component@latest/dist/dotlottie-player.mjs"
  type="module"></script>

<script>
  // Intersection Observer for animation triggers
  const observerOptions = {
    threshold: 0.1,
    rootMargin: '0px 0px -50px 0px',
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.style.animationPlayState = 'running';
      }
    });
  }, observerOptions);

  // Observe all fade-in elements
  document.addEventListener('DOMContentLoaded', () => {
    const fadeElements = document.querySelectorAll('.fade-in');
    fadeElements.forEach((el) => {
      el.style.animationPlayState = 'paused';
      observer.observe(el);
    });

    // Add staggered delay to feature cards
    const featureCards = document.querySelectorAll('.feature-card');
    featureCards.forEach((card, index) => {
      card.style.animationDelay = `${0.2 + index * 0.1}s`;
    });
  });

  // Smooth scrolling for anchor links
  document.querySelectorAll('a[href^="#"]').forEach((anchor) => {
    anchor.addEventListener('click', function (e) {
      e.preventDefault();
      const target = document.querySelector(this.getAttribute('href'));
      if (target) {
        target.scrollIntoView({
          behavior: 'smooth',
          block: 'start',
        });
      }
    });
  });
</script>
