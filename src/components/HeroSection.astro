<section class="hero">
  <div class="hero-container">
    <div class="hero-content">
      <div class="hero-text">
        <h1 class="headline fade-in delay-2">Seamless Field Service Management.<br>In Real-Time.</h1>
        <p class="subtitle fade-in delay-3">
          QIFESS provides all essential features for managing field service operations 
          with real-time tracking, scheduling, and analytics.
        </p>
        
        <div class="cta-buttons fade-in delay-4">
          <a href="#features" class="btn-primary">Explore Features</a>
          <a href="#demo" class="btn-secondary">Request Demo</a>
        </div>
        
        <div class="trust-badges fade-in delay-5">
          <div class="badge">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5.8 11.3L2 22l10.7-3.79"></path><path d="M4 3h.01M22 8h.01M15 2h.01M22 20h.01M22 2l-2.24 3.35a4 4 0 0 0 4.48 5.3 4 4 0 0 0-1.38 3.75 4 4 0 0 0-5.18 1.28 4 4 0 0 0-5.3-4.48A4 4 0 0 0 8.6 7.47a4 4 0 0 0-3.75 1.38 4 4 0 0 0-1.28-5.18"></path></svg>
            <span>Real-Time Tracking</span>
          </div>
          <div class="badge">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 7.5V6a2 2 0 0 0-2-2H5a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-1.5"></path><path d="M16 2v4"></path><path d="M8 2v4"></path><path d="M3 10h18"></path><path d="M8 14h.01"></path><path d="M12 14h.01"></path><path d="M16 14h.01"></path><path d="M8 18h.01"></path><path d="M12 18h.01"></path><path d="M16 18h.01"></path></svg>
            <span>Smart Scheduling</span>
          </div>
          <div class="badge">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"></path><polyline points="3.29 7 12 12 20.71 7"></polyline><line x1="12" y1="22" x2="12" y2="12"></line></svg>
            <span>Complete FSM Solution</span>
          </div>
        </div>
      </div>
      
      <div class="hero-image fade-in delay-4">
        <div class="image-container">
         <img src="/assets/img/qifess-mock.png" alt="Hero Image"/>
          
          <div class="floating-card card-1">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="22 12 16 12 14 15 10 15 8 12 2 12"></polyline><path d="M5.45 5.11L2 12v6a2 2 0 0 0 2 2h16a2 2 0 0 0 2-2v-6l-3.45-6.89A2 2 0 0 0 16.76 4H7.24a2 2 0 0 0-1.79 1.11z"></path></svg>
            <span>Field Tech Tools</span>
          </div>
          
          <div class="floating-card card-2">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2v4"></path><path d="M12 18v4"></path><path d="M4.93 4.93l2.83 2.83"></path><path d="M16.24 16.24l2.83 2.83"></path><path d="M2 12h4"></path><path d="M18 12h4"></path><path d="M4.93 19.07l2.83-2.83"></path><path d="M16.24 7.76l2.83-2.83"></path></svg>
            <span>Live Insights</span>
          </div>
        </div>
      </div>
    </div>
  </div>
  
  <div class="hero-bg-elements">
    <div class="blob blob-1"></div>
    <div class="blob blob-2"></div>
    <div class="grid-overlay"></div>
  </div>
</section>

<style>



  /* Hero Section */
  .hero {
    background: var(--bg-color);
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding: 5rem 2rem;
    position: relative;
    overflow: hidden;
  }

  .hero-container {
    max-width: 1200px;
    width: 100%;
    margin: 0 auto;
    color: var(--text-color);
    position: relative;
    z-index: 1;
  }

  .hero-content {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 4rem;
  }

  /* Text Section */
  .hero-text {
    flex: 1;
    text-align: left;
  }

  .headline {
    font-size: 3rem;
    line-height: 1.2;
    font-weight: 800;
    margin-bottom: 1.5rem;
    background: var(--text-color);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .subtitle {
    font-size: 1.2rem;
    line-height: 1.6;
    color: var(--text-light);
    margin-bottom: 2.5rem;
    max-width: 540px;
  }

  /* Button Styles */
  .cta-buttons {
    display: flex;
    gap: 1rem;
    margin-bottom: 2.5rem;
  }

  .btn-primary, .btn-secondary {
    padding: 14px 28px;
    border-radius: 12px;
    font-weight: 600;
    font-size: 1rem;
    transition: var(--transition);
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    justify-content: center;
  }

  .btn-primary {
    background-color: var(--primary-color);
    color: white;
    box-shadow: 0 10px 20px rgba(67, 97, 238, 0.3);
  }

  .btn-primary:hover {
    transform: translateY(-3px);
    box-shadow: 0 15px 25px rgba(67, 97, 238, 0.4);
  }

  .btn-secondary {
    background: white;
    color: var(--primary-color);
    border: 2px solid var(--primary-light);
  }

  .btn-secondary:hover {
    background: var(--primary-light);
    transform: translateY(-3px);
  }

  /* Trust Badges */
  .trust-badges {
    display: flex;
    gap: 1.5rem;
    flex-wrap: wrap;
  }

  .badge {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.9rem;
    color: var(--text-light);
    font-weight: 500;
  }

  .badge svg {
    color: var(--primary-color);
    width: 18px;
    height: 18px;
  }

  /* Image Section */
  .hero-image {
    flex: 1;
    position: relative;
  }

  .image-container {
    position: relative;
    height: 500px;
    width: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  /* Interactive Dashboard Mockup */
  .dashboard-mockup {
    width: 100%;
    max-width: 580px;
    height: 400px;
    background: white;
    border-radius: 16px;
    box-shadow: var(--shadow-lg);
    overflow: hidden;
    position: relative;
    z-index: 2;
  }

  .dashboard-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 16px 20px;
    border-bottom: 1px solid rgba(0,0,0,0.05);
  }

  .brand {
    font-weight: 700;
    color: var(--primary-color);
    font-size: 1.2rem;
  }

  .nav-dots {
    display: flex;
    gap: 6px;
  }

  .nav-dots span {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: #eaeaea;
  }

  .dashboard-content {
    display: flex;
    height: calc(100% - 54px);
  }

  .sidebar {
    width: 60px;
    background: #f9fafc;
    height: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    padding-top: 20px;
    gap: 20px;
  }

  .menu-item {
    width: 36px;
    height: 36px;
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
  }

  .menu-item.active {
    background: var(--primary-light);
  }

  .icon-circle {
    width: 20px;
    height: 20px;
    border-radius: 50%;
    background: #ddd;
  }

  .menu-item.active .icon-circle {
    background: var(--primary-color);
  }

  .main-area {
    flex: 1;
    padding: 20px;
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  .stats-row {
    display: flex;
    gap: 16px;
    width: 100%;
  }

  .stat-card {
    flex: 1;
    background: #f9fafc;
    border-radius: 12px;
    padding: 16px;
    position: relative;
    height: 100px;
    overflow: hidden;
  }

  .stat-card.highlight {
    background: linear-gradient(135deg, var(--secondary-color), var(--primary-color));
    color: white;
  }

  .stat-value {
    font-size: 1.8rem;
    font-weight: 700;
    margin-bottom: 4px;
  }

  .stat-label {
    font-size: 0.85rem;
    color: var(--text-light);
  }

  .stat-card.highlight .stat-label {
    color: rgba(255, 255, 255, 0.8);
  }

  .stat-chart {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 30px;
    background: linear-gradient(transparent, rgba(67, 97, 238, 0.1));
  }

  .map-container {
    flex: 1;
    background: #edf2f7;
    border-radius: 12px;
    position: relative;
    overflow: hidden;
  }

  .map-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: 
      linear-gradient(rgba(255,255,255,0.6) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255,255,255,0.6) 1px, transparent 1px);
    background-size: 20px 20px;
  }

  .location-pin {
    position: absolute;
    width: 16px;
    height: 16px;
    border-radius: 50%;
    background: var(--primary-color);
    transform: translate(-50%, -50%);
  }

  .pin1 {
    top: 30%;
    left: 20%;
  }

  .pin2 {
    top: 60%;
    left: 50%;
  }

  .pin3 {
    top: 40%;
    left: 80%;
  }

  .pulse {
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0% {
      box-shadow: 0 0 0 0 rgba(67, 97, 238, 0.7);
    }
    70% {
      box-shadow: 0 0 0 10px rgba(67, 97, 238, 0);
    }
    100% {
      box-shadow: 0 0 0 0 rgba(67, 97, 238, 0);
    }
  }

  .route-line {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    overflow: hidden;
  }

  .route-line::before {
    content: '';
    position: absolute;
    top: 30%;
    left: 20%;
    width: 60%;
    height: 2px;
    background: var(--primary-color);
    opacity: 0.7;
    clip-path: polygon(0 0, 50% 0, 50% 100%, 0 100%);
    animation: drawRoute 4s infinite;
    transform: translate(0, -50%);
  }

  @keyframes drawRoute {
    0% {
      clip-path: polygon(0 0, 0 0, 0 100%, 0 100%);
    }
    100% {
      clip-path: polygon(0 0, 100% 0, 100% 100%, 0 100%);
    }
  }

  /* Floating cards */
  .floating-card {
    position: absolute;
    background: white;
    padding: 12px 20px;
    border-radius: 12px;
    display: flex;
    align-items: center;
    gap: 10px;
    box-shadow: var(--shadow-md);
    font-size: 0.9rem;
    font-weight: 500;
    color: var(--text-color);
    animation: float 6s ease-in-out infinite;
    z-index: 3;
  }

  .floating-card svg {
    color: var(--primary-color);
  }

  .card-1 {
    top: 10%;
    left: -5%;
    animation-delay: 0s;
  }

  .card-2 {
    bottom: 15%;
    right: -5%;
    animation-delay: 1.5s;
  }

  @keyframes float {
    0% { transform: translateY(0px); }
    50% { transform: translateY(-15px); }
    100% { transform: translateY(0px); }
  }

  /* Background Elements */
  .hero-bg-elements {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    overflow: hidden;
    pointer-events: none;
  }

  .blob {
    position: absolute;
    background: linear-gradient(135deg, var(--primary-light), rgba(6, 214, 160, 0.1));
    border-radius: 50%;
    filter: blur(70px);
  }

  .blob-1 {
    width: 500px;
    height: 500px;
    top: -250px;
    right: -100px;
    opacity: 0.4;
  }

  .blob-2 {
    width: 600px;
    height: 600px;
    bottom: -300px;
    left: -200px;
    opacity: 0.3;
  }

  .grid-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: 
      linear-gradient(to right, rgba(67, 97, 238, 0.03) 1px, transparent 1px),
      linear-gradient(to bottom, rgba(67, 97, 238, 0.03) 1px, transparent 1px);
    background-size: 40px 40px;
  }

  /* Animation classes */
  .fade-in {
    opacity: 0;
    transform: translateY(20px);
    animation: fadeIn 0.7s ease forwards;
  }

  @keyframes fadeIn {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .delay-1 { animation-delay: 0.2s; }
  .delay-2 { animation-delay: 0.4s; }
  .delay-3 { animation-delay: 0.6s; }
  .delay-4 { animation-delay: 0.8s; }
  .delay-5 { animation-delay: 1s; }

  /* Responsive Design */
  @media (max-width: 1100px) {
    .hero-content {
      gap: 2rem;
    }
    
    .headline {
      font-size: 2.5rem;
    }
  }

  @media (max-width: 992px) {
    .hero-content {
      flex-direction: column;
    }
    
    .hero-text, .hero-image {
      width: 100%;
    }
    
    .hero-text {
      text-align: center;
      margin-bottom: 2rem;
    }
    
    .subtitle {
      margin: 0 auto 2.5rem auto;
    }
    
    .cta-buttons {
      justify-content: center;
    }
    
    .trust-badges {
      justify-content: center;
    }
    
    .dashboard-mockup {
      margin: 0 auto;
    }
  }

  @media (max-width: 768px) {
    .hero {
      padding: 6rem 1.5rem;
    }
    
    .headline {
      font-size: 2rem;
    }
    
    .subtitle {
      font-size: 1.1rem;
    }
    
    .cta-buttons {
      flex-direction: column;
      width: 100%;
      max-width: 300px;
      margin: 0 auto 2.5rem auto;
    }
    
    .btn-primary, .btn-secondary {
      width: 100%;
    }
    
    .floating-card {
      display: none;
    }
    
    .dashboard-mockup {
      height: 350px;
    }
  }

  @media (max-width: 480px) {
    .headline {
      font-size: 1.75rem;
    }
    
    .trust-badges {
      flex-direction: column;
      align-items: center;
      gap: 1rem;
    }
    
    .dashboard-mockup {
      height: 300px;
    }
    
    .stats-row {
      flex-direction: column;
      gap: 10px;
    }
    
    .stat-card {
      height: 80px;
    }
  }
</style>